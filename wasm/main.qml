import QtQuick

// The following imports seem to be needed so that the qmlimportscanner
// is able to list the plugins when calling qt6_import_qml_plugins() in
// CMakeLists.txt
import QtQuick.Layouts
import QtQuick.Controls

Rectangle {

    color: '#cccccc'

}
